# URL base de la API del backend.
# En Render (build), define esta misma variable en el Static Site para apuntar al backend desplegado.
# Si no se define, el código usa como fallback https://respawn-backend-521m.onrender.com/api
VITE_API_URL=http://localhost:3000/api
